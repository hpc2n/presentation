## <!-- .element: class="slideshow-title" --> Slurm at NAISS and HPC2N

<img src="../../img/logo.png" width="35%">
<br>
<small>2025-08-27</small>
<br>
<small>Birgitte Brydsö, HPC2N</small>



-----

### Overview

<small style="width: 85%; position: relative; bottom: 80px;">

- Site specifics 
- Serial, MPI, OpenMP jobs, Using GPUs
- Dependencies 
- Increasing the memory per task 
- Sample job scripts
    - I/O intensive
    - job arrays
- Job monitoring and efficiency
- Interactive work 

-----

### Site specifics 

<small style="width: 95%; position: relative; bottom: 80px;">

There are minor differences between the sites. 

- CPU/GPU types
- installed modules
- interactivity

Examples: Tetralith (NSC), Dardel (KTH), Kebnekaise (HPC2N) 

-----

### Site specifics - Tetralith 

<normal>

- 32 cores/node
- 96-384 GB RAM/node
- Nvidia T4 GPUs (1/node) 
    - 
```bash 
#SBATCH -n 1
#SBATCH -c 32
#SBATCH --gpus-per-task=1
```

-----

### Site specifics - Dardel 

- 128 cores/node
- 256-2048 GB RAM/node
- 4 AMD Instinct™ MI250X á 2 GCDs per node 
    - 
```bash 
#SBATCH -N 1
#SBATCH --ntasks-per-node=1
#SBATCH -p gpu
```

-----

#### Site specifics - Kebnekaise 

<small style="width: 95%; position: relative; bottom: 80px;">

- 28 cores/skylake, 72 cores/largemem, 128/256 cores/AMD Zen3/Zen4 
- 128-3072 GB RAM/node 
- NVidia V100, A100, A6000, L40s, H100, A40, AMD MI100 
    - 
```bash 
#SBATCH --gpus=x
#SBATCH -C <type>
```
    - type and number of cards/node (x) 
        - v100, 2
        - a40, 8
        - a6000, 2
        - l40s, 2 or 6
        - a100, 2
        - h100, 4
        - mi100, 2


-----

### Serial, MPI, and OpenMP jobs

Serial job: 

```bash 
#!/bin/bash 
#SBATCH -A <account>
#SBATCH -t HHH:MM:SS 
#SBATCH -n 1

module load <modules>

./myserialprogram  
```

- n, number of tasks. Since cores per task is 1 as default, this equals cores in this case. 

-----

#### Note: Time/walltime

<small style="width: 95%; position: relative; bottom: 80px;"> 

- the job **will** terminate when the time runs out, whether it has finished or not 
- you will only be "charged" for the consumed time
- asking for more time than needed will make the job take longer to start
- short jobs can start quicker (backfill)
- if you have no idea how long your job takes, ask for "long" time 
- Conclusion: Ask for "a bit" more time than needed, but not too much

-----

### Serial, MPI, and OpenMP jobs

<small style="width: 95%; position: relative; bottom: 80px;"> 

MPI job: 

```bash
#!/bin/bash 
#SBATCH -A <account>
#SBATCH -t HHH:MM:SS 
#SBATCH -n <tasks> 

module load <modules>

srun ./mympiprogram
```

- Other options are asking for whole nodes (- N) and possibly asking for ``--tasks-per-node`` 
- ``srun`` and ``mpirun`` should be interchangeable at HPC2N. Tetralith uses ``mpprun`` and Dardel uses ``srun`` 
- Remember you need to load a module with MPI 

-----

### Serial, MPI, and OpenMP jobs

<small style="width: 95%; position: relative; bottom: 80px;">

OpenMP job: 

```bash
#!/bin/bash 
#SBATCH -A <account>
#SBATCH -t HHH:MM:SS 
#SBATCH -c <cores-per-task> 

module load <modules>

# Set OMP_NUM_THREADS to the same value as -c with a fallback in case it isn't set.
# SLURM_CPUS_PER_TASK is set to the value of -c, but only if -c is explicitly set
if [ -n "$SLURM_CPUS_PER_TASK" ]; then
  omp_threads=$SLURM_CPUS_PER_TASK
else
  omp_threads=1
fi
export OMP_NUM_THREADS=$omp_threads

./myopenmpprogram
```

- ``-c`` is used to set cores per task and should be the same as ``OMP_NUM_THREADS`` 

-----

### Using GPUs

<small style="width: 95%; position: relative; bottom: 80px;">

This is the most different of the Slurm settings, between centers. 

| Resource | batch settings | 
| -------- | -------------- | 
| Tetralith | ``#SBATCH -n 1`` <br> ``#SBATCH -c 32`` <br> ``#SBATCH --gpus-per-task=1`` |
| Dardel    | ``#SBATCH -N 1`` <br> ``#SBATCH --ntasks-per-node=1`` <br> ``#SBATCH -p gpu`` | 
| Kebnekaise | ``#SBATCH --gpus=x`` <br> ``#SBATCH -C <type>`` | 

-----

### Dependencies

Sometimes your workflow has jobs that depend on a previous job 

- Submit your first job: ``sbatch myjob.sh``
- Wait for that job to finish before starting next job: ``sbatch -d afterok:<prev-JOBID> mynextjob.sh``

Several jobs connected like this -> scripting!  

-----

### Increasing the memory per task 

<small style="width: 95%; position: relative; bottom: 80px;">

- Running out of memory ("OOM"): 
    - usually the job stops ("crashes") 
    - check the Slurm error/log files
    - check with sacct/seff/jobstats/job-usage depending on cluster 
- Fixes:
    - use "fat" nodes
    - allocate more cores just for memory 
    - tweak mem usage in app, if possible 

-----

#### Increasing memory per task - Tetralith 

96 GB/node: 
```bash
-C thin --exclusive
``` 

384 GB/node: 
```bash
-C fat --exclusive
```

More cores/task, some just giving memory (MPI): 
```bash
sbatch --ntasks-per-node=16 --cpus-per-task=2
```

-----

#### Increasing memory per task - Dardel 

<small style="width: 95%; position: relative; bottom: 80px;">

| Type | RAM | Partition | Available | Example flag |
| ---- | --- | --------- | --------- | ------------ |  
| Thin | 256 GB | main, shared, long | 227328 MB | |  
| Large | 512 GB | main, memory | 456704 MB | --mem=440GB | 
| Huge | 1 TB |	main, memory | 915456 MB | --mem=880GB | 
| Giant | 2 TB | memory | 1832960 MB | --mem=1760GB |
| GPU | 512 GB | gpu | 456704 MB | --mem=440GB | 

On shared partitions you need to give number of cores and will get RAM equivalent for that 

-----

#### Increasing memory per task - Kebnekaise 

<small style="width: 95%; position: relative; bottom: 80px;">

| Type | RAM/core | cores/node | requesting flag | 
| ---- | -------- | ---------- | --------------- | 
| Intel Skylake | 6785 MB | 28 | -C skylake | 
| AMD Zen3 | 8020 MB | 128 | -C zen3 | 
| AMD Zen4 | 2516 MB | 256 | -C zen4 | 
| V100 | 6785 MB | 28 | --gpus=<#> -C v100 | 
| A100 | 10600 MB | 48 | --gpus=<#> -C a100 | 
| MI100 | 10600 MB | 48 | --gpus=<#> -C mi100 | 
| A6000 | 6630 MB | 48 | --gpus=<#> -C a6000 | 
| H100 | 6630 MB | 96 | --gpus=<#> -C h100 | 
| L40s | 11968 MB | 64 | --gpus=<#> -C l40s | 
| A40 | 11968 MB | 64 | --gpus=<#> -C a40 | 
| Largemem | 41666 MB | 72 | -C largemem | 

<hr>
- Also more cores/task with ``-c <#cores/task>`` just for memory

-----

### I/O intensive jobs

<small style="width: 95%; position: relative; bottom: 80px;">

- In most cases, use the project storage
- If needed, use node-local disk 
- The environment variable ``$SNIC_TMP`` points to the job directory on the compute node
    - On some systems $TMPDIR also points to the node local disk 
- The environment variable ``$SLURM_SUBMIT_DIR`` is the directory you submitted from 

-----

### I/O intensive jobs - example 

<small style="width: 95%; position: relative; bottom: 80px;">

```bash
#!/bin/bash 
#SBATCH -A <account>
#SBATCH -t HHH:MM:SS 
#SBATCH -n <cores>

module load <modules>

# Copy your data etc. to node local scratch disk
cp -p mydata.dat $SNIC_TMP
cp -p myprogram $SNIC_TMP 

# Change to that directory
cd $SNIC_TMP

# Run your program
./myprogram

# Copy the results back to the submission directory 
cp -p mynewdata.dat $SLURM_SUBMIT_DIR 
``` 

When using node local disk it is important to remember to copy the output data back, since it will go away when the job ends!

-----

### Multiple jobs, same job script

<small style="width: 95%; position: relative; bottom: 80px;">

- you have several (perhaps smaller) programs to run
- you can just have the jobs wait in queue ones for all of them
- they are run from the same job script and have similar requirements
- if you have a few, just list all in the same script
- if you have many, job arrays or make a script to submit are better 

-----

### Multiple serial jobs, simultaneously 

<small style="width: 95%; position: relative; bottom: 80px;">

```bash
#!/bin/bash
#SBATCH -A <account>
# Add enough cores that all jobs can run at the same time 
#SBATCH -n <cores>
# Make sure the time is long enough that the longest job have time to finish 
#SBATCH --time=HHH:MM:SS

module load <modules> 

srun -n 1 --exclusive ./program data1 &
srun -n 1 --exclusive ./program2 data2 & 
srun -n 1 --exclusive ./program3 data3 &
... 
srun -n 1 --exclusive ./program4 data4 &
wait
```

-----

### Multiple simultaneous jobs (serial or parallel) 

<small style="width: 95%; position: relative; bottom: 80px;">

In this example, 3 jobs each with 14 cores 

```bash 
#!/bin/bash
#SBATCH -A <account>
# Since the files run simultaneously I need enough cores for all of them to run
#SBATCH -n 56
# Remember to ask for enough time for all jobs to complete
#SBATCH --time=00:10:00

module load <modules>

srun -n 14 --exclusive ./mympiprogram data &
srun -n 14 --exclusive ./my2mpi data2 &
srun -n 14 --exclusive ./my3mpi data3 &
wait
```

-----

### Multiple sequential jobs (serial or parallel) 

<small style="width: 95%; position: relative; bottom: 80px;">

This example is for jobs where some are with 14 tasks with 2 cores per task and some are 4 tasks with 4 cores per task 

```bash
#!/bin/bash
#SBATCH -A <account>
# Since the programs run sequentially I only need enough cores for the largest of them to run
#SBATCH -c 28
# Remember to ask for enough time for all jobs to complete
#SBATCH --time=HHH:MM:SS

module load <modules>

srun -n 14 -c 2 ./myprogram data 
srun -n 4 -c 4 ./myotherprogram mydata
...
srun -n 14 -c 2 ./my2program data2 
```

-----

### Job arrays

<small style="width: 95%; position: relative; bottom: 80px;">

- Job arrays offer a mechanism for submitting and managing collections of similar jobs. 
- All jobs must have the same initial options (e.g. size, time limit, etc.), however it is possible to change some of these options after the job has begun execution using the scontrol command specifying the JobID of the array or individual ArrayJobID.
- the execution times vary depending on input data
- You create multiple jobs from one script, using the ``-- array`` directive. 
- requires little BASH scripting abilities 
- max number of jobs restricted by max number of jobs/user 

- More information here on the official Slurm documentation pages: <a href="https://slurm.schedmd.com/job_array.html" target="_blank">https://slurm.schedmd.com/job_array.html</a>.

-----

### Job arrays - simple example 

<small style="width: 95%; position: relative; bottom: 80px;">

- a small Python script ``hello-world-array.py`` 

```bash
# import sys library (we need this for the command line args)
import sys

# print task number
print('Hello world! from task number: ', sys.argv[1])
```

- a batch script ``hello-world-array.sh``

```bash
#!/bin/bash
# A very simple example of how to run a Python script with a job array
#SBATCH -A <account>
#SBATCH --time=00:05:00 # Asking for 5 minutes
#SBATCH --array=1-10   # how many tasks in the array
#SBATCH -c 1 # Asking for 1 core    # one core per task
# Create specific output files for each task with the environment variable %j
# which contains the job id 
#SBATCH -o hello-world-%j-%a.out

# Load any modules you need, here for Python 3.11.3 on Kebnekaise
module load GCC/12.3.0 Python/3.11.3

# Run your Python script
srun python hello-world-array.py $SLURM_ARRAY_TASK_ID
```

-----

### Job arrays - comments 

<small style="width: 95%; position: relative; bottom: 80px;">

- You can either go with the default step of 1 
    - Example: ``#SBATCH --array=4-80``
- or give an index (here steps of 4) 
    - Example: ``#SBATCH --array=1-100:4``
- and you can give a list instead of a range 
    - Example: ``#SBATCH --array=5,8,33,38``
- you can throttle jobs, so only a smaller number of jobs run at a time
    - Example: ``#SBATCH --array1-400%4``
- Name the output and error files so each step gets their own 
    - ``#SBATCH -o output_%j.out`` or ``#SBATCH -o process_%A_%a.out``
    - ``#SBATCH -e error_%j.err`` or ``#SBATCH -e process_%A_%a.err`` 
- There is an environment variable ``$SLURM_ARRAY_TASK_ID`` which can be used to check/query with 

-----

### Job monitoring and efficiency

<small style="width: 95%; position: relative; bottom: 80px;">

There are several commands to monitor the job, some of which are centre specific: 

- ``scontrol show job JOBID``
- ``squeue --me --start`` 
- ``jobinfo`` (wrapper around squeue)

- ``jobstat``
- ``sacct``
- ``job-usage``

-----

### Interactive work

-----

## An example of flowchat

See Ref [1] for more examples.

----

--flowchart--

----

## An example of mindmap

See Ref [2] for more examples.

----

--mindmap--

----

## An example of quizz

----

---quizz---

-----

## References

<small>
- [1] *Flowchart.js*, available at http://flowchart.js.org/.
- [2] *JsMind*, available at https://github.com/hizzgdev/jsmind/.

</small>

